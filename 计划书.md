**一、项目选题与背景**

本项目选题为“ABACUS平面波生成与应用的并行优化”，覆盖8个基础任务与1个附加题，围绕平面波分布、FFT变换、MPI通信重排、GammaOnly存储与算法等核心流程开展系统优化。ABACUS采用平面波基组作为电子结构计算的核心表示形式，其计算量与平面波数量成正比，同时FFT、gather/scatter、k点并行与通信同步构成主要瓶颈。当前代码已具备基础并行框架（MPI+OpenMP），但仍存在串行热区、阻塞通信、数据布局不友好、GammaOnly仅部分可用等问题。通过面向这些热点的并行优化与结构扩展，可显著提升计算效率、降低内存占用，并为多k点/高并行场景提供可扩展的改进路径。

**二、研究目标与核心内容**

目标是完成以下三类提升：

1）**并行效率**：提升平面波分布统计、FFT前后拷贝重排、gather/scatter的并行利用率，减少阻塞等待，提升线程与进程扩展性；

2）**通信效率**：把Alltoallv等阻塞通信替换为非阻塞，实现通信与计算重叠；

3）**内存效率**：在GammaOnly条件下实现紧凑存储与半谱重构，减少50%级别的倒空间存储开销。

核心内容对应8题与附加题：
- 题目1：`count_pw_st`的OpenMP并行化
- 题目2：`gatherp_scatters`的非阻塞通信
- 题目3：FFT变换数据拷贝/重排的OpenMP优化
- 题目4：多k点GammaOnly支持
- 题目5：gather/scatter重排循环的SIMD向量化
- 题目6：电荷密度/势/波函数的GammaOnly紧凑存储
- 题目7：FFT过程中的通信-计算重叠
- 题目8：G矢量与|g+k|^2的缓存优化
- 附加题：完整GammaOnly算法（含r2c/c2r半谱逻辑）

**三、相关基础与技术原理**

1）**平面波与FFT**：平面波系数与实空间波函数通过FFT相互转换，计算流程中需要频繁进行实/倒空间变换。FFT的性能受到数据布局、内存访问与跨进程通信影响。

2）**MPI通信机制**：当前`gatherp_scatters`使用阻塞`MPI_Alltoallv`，导致全局同步等待。非阻塞`MPI_Isend/Irecv`或`MPI_Ialltoallv`可用于通信与计算的重叠。

3）**OpenMP并行与SIMD**：计数与重排循环适合采用OpenMP并行与`#pragmaompsimd`向量化指令，以提高单节点吞吐。

4）**GammaOnly原理**：Gamma点下波函数为实函数，其倒空间满足共轭对称$F(-G)=F^*(G)$，可只存储一半谱（半谱），理论上可节省50%内存并减少FFT规模。

5）**缓存与复用**：在SCF迭代中，G矢量、|g+k|^2等与结构、k点相关但与迭代步无关的数据适合缓存复用。

**四、总体方案与设计思路**
整体采用“分阶段、可验证、可回退”的策略，保证每一项优化均可独立验证正确性与性能收益：

- **阶段A：OpenMP并行化与局部优化**（题1、题3）
  对`count_pw_st`三重循环进行并行化与线程安全处理，并优化FFT前后数据拷贝/重排的内存访问模式与调度策略。
- **阶段B：MPI通信与重叠**（题2、题7）
  将阻塞通信替换为非阻塞，进一步引入双缓冲或流水线模式，使通信与部分计算重叠。
- **阶段C：SIMD与缓存**（题5、题8）
  对gather/scatter重排逻辑进行向量化，建立G矢量、|g+k|^2等不可变数据的缓存机制。
- **阶段D：GammaOnly扩展**（题4、题6、附加题）
  扩展`PW_Basis_K`支持多k点下的GammaOnly，并设计紧凑存储格式与半谱变换接口，最终实现完整GammaOnly算法。
- **阶段E：测试与报告**
  完整的单元测试与性能基准覆盖全部功能，并输出量化数据与总结。

**五、实现步骤与关键难点**

1）**count_pw_st并行化**：

难点在于并行统计的线程安全与结果一致性，需要确认循环依赖，采用私有累加或分块归并，保证输出与串行一致。此外，将平面波判断逻辑抽象为独立的函数对象，提高代码的可扩展性。

2）**FFT变换优化**：

重排循环的内存访问是性能关键，需要在不改变算法语义的前提下调整looporder、schedule策略、并行块大小以提升缓存命中。

3）**非阻塞通信与重叠**：

难点在于通信缓冲区管理、请求对象生命周期以及正确的同步点安排。若`MPI_Ialltoallv`不可用则采用`Isend/Irecv`显式通信方案。

4）**SIMD向量化**：

需要确保数据对齐与连续性，避免对齐失效或假共享导致收益不明显。采用pragma方式优先，必要时引入平台特定优化。

5）**GammaOnly扩展**：

多k点中并非所有k点都满足GammaOnly条件，需要加入对k点集合的判定逻辑，确保仅在对称性允许时启用。紧凑存储要求定义新数据布局与pack/unpack逻辑，并保持与现有接口兼容。

6）**缓存机制**：

难点是缓存失效时机与线程安全。需明确缓存生命周期（如结构或k点变化时重建）并提供可靠的无副作用访问接口。

**六、测试方案与预期结果**

测试体系分为“正确性测试+性能基准测试”。

**1）正确性测试**

- 串行/并行对比：所有并行优化模块（计数、重排、FFT）输出完全一致或误差小于$10^{-6}$
- GammaOnly对比：对比GammaOnly与非GammaOnly计算结果，差异控制在$10^{-6}$
- 紧凑存储round-trip：压缩/解压与原始数据一致
- 测试count_pw_st在不同网格大小和截断能量下的边界情况

**2）性能测试配置**

- 线程数：1、2、4、8
- 进程数：1、2、4、8
- 测试体系：Alfcc、Sidiamond、NaClrocksalt、Febcc（自旋极化体系）等
- 计时指标：`distributeg`、`real2recip`、`recip2real`、`gather/scatter`、端到端总时间

**3）预期指标**

- `count_pw_st`：4线程目标4x加速
- 通信优化：阻塞->非阻塞，端到端2x级别提升
- FFT数据拷贝：4线程目标4x加速
- GammaOnly存储：约50%内存节省

**七、进度安排与分工**

- 4.24（第10周）项目调研与计划书（3分）
  - A：梳理8题+附加题总体技术路线，列出核心模块与依赖
  - B：整理性能瓶颈与并行点（OpenMP/MPI/SIMD/GammaOnly）
  - C：整理测试体系与基准算例建议
  - 产出：项目计划书（已完成版）
- 5.15（第13周）详细算法文档（3分）
  - A：题1/3/5（OpenMP+SIMD）算法细化
  - B：题2/7（MPI非阻塞+重叠）算法细化
  - C：题4/6/附加题（GammaOnly扩展与紧凑存储）算法细化
  - 产出：详细算法文档（模块化说明、流程图、关键数据结构）
- 5.22（第14周）当前算法测试与总结（3分）
  - A：完成题1/3/5的测试用例与结果
  - B：完成题2/7的测试用例与结果
  - C：完成题4/6/附加题的正确性验证设计
  - 产出：阶段性测试报告（正确性+初步性能基线）
- 5.29（第15周）代码修改与重构报告（3分）
  - A：总结OpenMP/SIMD相关代码改动与重构点
  - B：总结MPI非阻塞与重叠改动与重构点
  - C：总结GammaOnly扩展与紧凑存储改动与重构点
  - 产出：代码修改/重构报告（含关键文件与函数）
- 6.5（第16周）代码优化报告（3分）
  - A：性能对比（OpenMP/SIMD方向）
  - B：性能对比（MPI重叠方向）
  - C：内存节省与GammaOnly效果评估
  - 产出：优化报告（加速比、瓶颈对比、改进分析）
- 6.12（期末）最终优化效果与总结（5分）
  - A：整合性能曲线与实验结果
  - B：整合正确性验证与回归测试
  - C：整合工程化总结与未来改进建议
  - 产出：最终报告与答辩材料

**八、参考文献与资料**

- ABACUS源代码与官方文档
- FFTW文档：Real-to-Complex/Complex-to-RealFFT
- MPI标准文档：非阻塞通信与Alltoallv
- OpenMP文档：并行与SIMD指令
- 平面波与GammaOnly相关理论教材与论文
