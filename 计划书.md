# ABACUS 平面波基组并行模块设计与优化项目计划书

## 一、项目选题与背景

本项目题目为 **ABACUS 平面波基组并行模块设计与优化**，依托 ABACUS 软件中的平面波基组并行模块 `source/source_basis/module_pw`，围绕平面波分布统计、FFT 实/倒空间变换、MPI 通信重排、GammaOnly 存储与半谱算法等核心流程，开展面向高性能计算的系统性并行优化。

当前 `module_pw` 模块已具备 MPI+OpenMP 两级并行框架，主要涵盖：

- `PW_Basis`：单 k 点平面波基组，管理 G 矢量、FFT 网格与索引映射
- `PW_Basis_K`：多 k 点平面波基组
- `pw_distributeg.cpp`：平面波分布，含 `count_pw_st` 与 `distribute_g`
- `pw_transform.cpp`：实/倒空间 FFT 变换
- `pw_gatherscatter.h`：MPI 通信与数据重排

然而，现有实现在高并行度与大体系场景下仍存在显著瓶颈：

- `count_pw_st` 中统计截断球内平面波数量的三重嵌套循环为串行执行，且每次迭代涉及矩阵向量乘法，初始化阶段随体系增大显著变慢；
- `gatherp_scatters` 依赖阻塞式 `MPI_Alltoallv`，通信期间进程空等，无法与局部计算重叠；
- FFT 前后的数据拷贝与索引重排存在访存不连续、缓存命中率低等问题；
- G 矢量模长等数据在迭代中重复计算却未做缓存复用；
- gather/scatter 局部重排循环未利用 SIMD 向量化。
- GammaOnly 优化尚未完整启用。当 k 点仅取 Gamma 点时，实空间波函数满足时间反演对称，其傅里叶分量具有共轭对称性 $F(-\mathbf{G}) = F^*(\mathbf{G})$，理论上可减少约 50% 的倒空间存储与带宽。
当前 `PW_Basis` 已支持 `gamma_only` 标志及半谱 FFT，但 `PW_Basis_K` 中多 k 点场景下 GammaOnly 被强制关闭，紧凑存储格式亦未在电荷密度、势函数、波函数中系统化实现。

针对上述串行热区、阻塞通信、数据布局不友好、GammaOnly 仅部分可用等问题，开展并行优化与结构扩展，可显著提升计算效率、降低内存占用，并为多 k 点/高并行场景提供可扩展的改进路径。

## 二、研究目标与核心内容

研究目标是完成以下三类提升：

1. **并行效率**：提升平面波分布统计、FFT 前后拷贝重排、gather/scatter 的并行利用率，减少阻塞等待，提升线程与进程扩展性；

2. **通信效率**：把 Alltoallv 等阻塞通信替换为非阻塞，实现通信与计算重叠；

3. **内存效率**：在 GammaOnly 条件下实现紧凑存储与半谱重构，减少 50% 级别的倒空间存储开销。

核心内容对应大作业说明中的 8 道题目与 1 道附加题，具体包括：

- **题目 1：`count_pw_st` 的 OpenMP 并行化**。针对 `pw_distributeg.cpp` 中统计截断球内平面波数量的三重循环，分析循环依赖与线程安全，采用 OpenMP 并行化与归约操作提升初始化阶段效率，并将平面波判断逻辑抽象为可测试的判据接口。

- **题目 2：`gatherp_scatters` 的非阻塞通信**。针对 `pw_gatherscatter.h` 中的阻塞 `MPI_Alltoallv`，使用 `MPI_Isend`/`MPI_Irecv` 或 `MPI_Ialltoallv` 实现非阻塞通信，在等待期间执行局部数据重排，实现通信与计算重叠。

- **题目 3：FFT 变换数据拷贝/重排的 OpenMP 优化**。针对 `pw_transform.cpp` 中 `real2recip` 与 `recip2real` 的数据拷贝和重排循环，优化内存访问模式、调度策略与缓存局部性，提升线程级并行效率。

- **题目 4：多 k 点 GammaOnly 支持**。扩展 `PW_Basis_K`，解除当前 `assert(gamma_only == false)` 限制，使多 k 点场景在物理条件允许时（如所有 k 点均为 Gamma 点）能够启用 GammaOnly，实现兼容的半谱 FFT 接口。

- **题目 5：gather/scatter 重排循环的 SIMD 向量化**。针对 `pw_gatherscatter.h` 中两处数据重排循环，使用 `omp simd` 或 Intrinsics 实现 SIMD 向量化，提升单核吞吐。

- **题目 6：电荷密度/势/波函数的 GammaOnly 紧凑存储**。设计紧凑存储格式，对 $\rho(\mathbf{G})$、$V(\mathbf{G})$、$\psi(\mathbf{G})$ 仅存储半谱独立分量，提供透明的 pack/unpack 接口，保持与现有代码兼容。

- **题目 7：FFT 过程中的通信-计算重叠**。在 FFT 全流程中引入双缓冲机制，使 Z-FFT 准备、局部系数提取等计算与 MPI 通信重叠。

- **题目 8：G 矢量与 $|g+k|^2$ 的缓存优化**。在 `PW_Basis` 中建立懒加载缓存机制，对 G 矢量笛卡尔坐标、模长平方等跨 SCF 迭代不变的数据实现一次性计算与复用。

- **附加题：完整 GammaOnly 算法**。系统化实现 GammaOnly 下的完整算法，包括 FFT 网格半谱设置、紧凑数据结构的构造与展开、r2c/c2r 变换及 SIMD 加速的数据转换，确保能量误差低于 $10^{-6}$ Ry。

## 三、相关基础与技术原理

我们不妨从“科学基础”和“技术原理”两个角度展开讲（这部分参考了`01_plane_wave.md`和ABACUS官方仓库的代码）：

1. **平面波基组表示**：
    在周期性体系的密度泛函理论计算中，电子波函数可写成平面波展开形式：

    $$\psi_{n\mathbf{k}}(\mathbf{r})=\sum_{\mathbf{G}}c_{n,\mathbf{k}+\mathbf{G}}e^{i(\mathbf{k}+\mathbf{G})\cdot\mathbf{r}}$$

    其中，$\mathbf{k}$为布里渊区采样点，$\mathbf{G}$为倒格矢，$c_{n,\mathbf{k}+\mathbf{G}}$为平面波系数。实际计算时会根据截断能量$E_{cut}$选取满足$\frac{|\mathbf{k}+\mathbf{G}|^2}{2} < E_{cut}$ 的有限个平面波，而平面波数量直接决定倒空间向量规模、FFT网格规模、内存占用与循环结构计算量。ABACUS中，相关基础数据结构主要包括单k点平面波基组`PW_Basis`、多k点平面波基组`PW_Basis_K`以及平面波分布等相关模块。

2. **实空间/倒空间变换与FFT**：
    平面波方法的核心优势，是可通过快速傅里叶变换（FFT）在实空间与倒空间之间切换。波函数、密度和势函数在倒空间中可以施加动能算符或处理$G$矢量相关项，在实空间中则可以进行局域势、交换相关势等逐点操作。因此一次自洽场（SCF）迭代中，会频繁出现“倒空间系数$\rightarrow$实空间函数$\rightarrow$实空间操作$\rightarrow$倒空间系数”的往返变换。ABACUS中，`pw_transform.cpp`等模块负责相关变换。

3. **k点采样与Gamma点**：
    布里渊区积分需要多个离散k点完成（Bloch定理）。k点数量越多，能带、总能和电荷密度等物理量越精确，当然计算量也会随之增加。

    Gamma点是$\mathbf{k}=0$的布里渊区中心点，在大晶胞、非金属体系或初步结构优化中常被采用。当k点只取Gamma点（即GammaOnly）时，实空间波函数可视为实函数，其傅里叶分量满足共轭对称关系：

    $$F(-\mathbf{G})=F^*(\mathbf{G})$$

    这一性质也是GammaOnly优化的理论基础（我们下一小块会讲到）。注意，一些条件下（比如金属体系的精细费米面采样、自旋轨道耦合、外磁场或部分自旋）GammaOnly并不适用，因此实现时必须先进行物理与输入条件判定。

4. **GammaOnly半谱存储**：
    在通常的复数FFT和平面波存储中，正负$\mathbf{G}$分量都显式保存；而GammaOnly模式下，只需存储一半独立谱分量，另一半可由共轭对称关系恢复。理论上，倒空间波函数、密度和势函数的存储可接近减少50%，相应的数据搬移、内存带宽压力和部分FFT规模也会下降。ABACUS库中，已经有`gamma_only`标志及FFT网格半谱处理逻辑，例如在特定方向上令`fftnx = nx/2 + 1`或`fftny = ny/2 + 1`。

5. **通信与计算重叠**：
    平面波和FFT网格在多进程下需要按进程划分，然而倒空间分布、实空间网格分布和FFT库期望的数据布局并不总是一致，因此计算过程中需要大量gather/scatter与All-to-All类型通信。

    目前`gatherp_scatters`等路径主要使用阻塞通信`MPI_Alltoallv`。非阻塞`MPI_Ialltoallv`或显式`MPI_Isend/Irecv`可将通信请求提前发出，把可独立完成的拷贝、局部重排或计算安排在等待期间，实现通信与计算重叠。

6. **OpenMP并行与SIMD向量化**：
    在单节点内部，平面波计数、FFT前后数据拷贝、索引重排和gather/scatter局部循环具有较强的数据并行性。对`count_pw_st`等循环可采用线程私有计数、分块归并以及reduction保证线程安全；而对连续内存拷贝和规则索引访问，可使用OpenMP并行循环、合理的`schedule`策略以及`#pragma omp simd`向量化提示提升吞吐。

7. **缓存复用与数据局部性**：
    在SCF迭代中，部分数据由晶格、截断能、k点集合和FFT网格决定，例如$\mathbf{G}$矢量、$|\mathbf{G}+\mathbf{k}|^2$、索引映射表和通信计数/位移数组。这些数据在结构或k点不变时，可以跨迭代复用，避免重复生成和重复遍历，这就是缓存复用。当然，我们需要明确生命周期与失效条件，尽量保持只读访问，以便在线程并行和多进程通信中保持一致性。

## 四、总体方案与设计思路

我们决定，暂且将整体计划分为以下几个工作流（如果实际工作时，工作量差别很大，我们会自主重新调度），每一个工作流单独开分支，便于验证每一项优化的正确性与性能提升。最终，我们会将各部分整合好，并验证整体收益，向ABACUS官方仓库提交PR。

- **工作流A：OpenMP并行化与局部优化**（题1、题3，姚远）
  对`count_pw_st`三重循环进行并行化与线程安全处理；优化FFT前后数据拷贝/重排的内存访问模式与调度策略。
- **工作流B：MPI通信与计算重叠**（题2、题7，徐洋）
  将阻塞通信优化为非阻塞通信，进一步引入双缓冲或流水线模式，尽可能实现更多部分的通信与计算重叠。
- **工作流C：SIMD向量化与缓存复用**（题5、题8，徐贤天）
  对gather/scatter的重排逻辑进行向量化；建立G矢量、|g+k|^2等不可变数据的缓存机制。
- **工作流D：GammaOnly扩展**（题4、题6、附加题，All）
  扩展`PW_Basis_K`，使之支持多k点下的GammaOnly，并设计紧凑存储格式与半谱变换接口。最终我们的期望是实现完整GammaOnly算法，据说风险较高，还需评估。
- **工作流E：测试与报告**
  进行完整的测试，输出量化数据，进行总结。

## 五、实现步骤与关键难点

1. **count_pw_st并行化**：

    难点在于并行统计的线程安全与结果一致性，需要确认循环依赖，采用私有累加或分块归并，保证输出与串行一致。此外，将平面波判断逻辑抽象为独立的函数对象，提高代码的可扩展性。

2. **FFT变换优化**：

    重排循环的内存访问是性能关键，需要在不改变算法语义的前提下调整looporder、schedule策略、并行块大小以提升缓存命中。

3. **非阻塞通信与重叠**：

    这部分我的理解就是将阻塞通信的相关函数改为非阻塞通信的相关函数，麻烦之处可能在于通信缓冲区管理、正确的同步点安排以及与其他部分的联系（比如缓存等）。

4. **SIMD向量化**：

    需要确保数据对齐与连续性，避免对齐失效或假共享导致收益不明显。采用pragma方式优先，必要时引入平台特定优化。

5. **GammaOnly扩展**：

    当前的Gamma Only仅支持所有k点都是Gamma点的情况，而不支持混合k点（Gamma+非Gamma）的情况。如果想要实现混合k点，则需要把 `pw_basis_k`里的 `gamma_only`的判断从全局判断改为判断每个点是否是Gamma点。

    此后的改造点：

    - 让Gamma k点走r2c和c2r，而让非Gamma k点走complex FFT
    - 改变`distribute_g`和`count_pw_st`里的x/y扫描范围，按照Gamma点和非Gamma点分类
    - `initfft`的初始化也要重新设计，按照Gamma非Gamma分类

    改造后的下游测试，需要覆盖全Gamma、混合k点两类的场景。

    在Gamma Only的存储优化方面，要求设计一个紧凑的存储格式，此后需修改相关的函数来支持此格式。此外要求定义pack/unpack逻辑，同时保持和现有接口兼容。

6. **缓存机制**：

    难点是缓存失效时机与线程安全。需明确缓存生命周期（如结构或k点变化时重建）并提供可靠的无副作用访问接口。

## 六、测试方案与预期结果

测试体系分为“正确性测试+性能基准测试”，在确保代码正确的基础上尽可能提升性能。

1. **正确性测试**

   - 串行/并行对比：所有并行优化模块（计数、重排、FFT）输出完全一致或误差小于$10^{-6}$
   - Gamma Only对比：对比Gamma Only与非Gamma Only计算结果，差异控制在$10^{-6}Ry$
   - 紧凑存储round-trip：压缩/解压与原始数据一致
   - 测试count_pw_st在不同网格大小和截断能量下的边界情况

2. **性能测试配置**

   - 进程数分别取1、2、4、8，线程数也分别取1、2、4、8，观察不同的进程和线程分配对计算效率的影响，寻找最优的进程线程分配

   - 测试体系选择：

     - Al fcc（面心立方）
       - 这是简单金属，此体系的电子波函数比较平滑，但为了保证精度需要较高的ecut，对平面波的截断能比较敏感，适合测试count_pw_st的并行效率

     - Si diamond（金刚石结构）
       - 这是半导体，原子之间由共价键相连。电荷密度分布均匀，适合验证Gamma Only的正确性

     - NaCl rocksalt（岩盐结构）
       - 这是离子晶体，具有大晶胞，原子序数差异较大，网络格点个数多，适合测试FFT通信和重排的效率

     - Fe bcc（体心立方）
       - 这是磁性金属，具有自旋极化的特性，不具有时间反演对称性，用于检验Gamma Only模块能力

   - 具体各个题目的测试体系初步安排

     - 题目1：使用Al测试count_pw_st在OpenMP并行后的效率和加速比

     - 题目2：使用NaCl，观察在高负载的FFT变换下，使用非阻塞通信是否能够有效减小通信的开销

     - 题目3：使用Al和NaCl，先用Al检测并行逻辑的正确性，再用NaCl测试OpenMP优化下的FFT的加速比

     - 题目4：首先使用Si或NaCl，他们具有时间反演对称性，可以直接使用Gamma Only，用它们来验证代码正确性，以及开启Gamma Only后内存占用是否降低50%；其次使用Fe，具有自旋极化特性，这破坏了在Gamma点的时间反演对称性，不支持开启Gamma Only，用这个体系来测试Gamma Only模式对自旋极化体系是否能正确识别并退出Gamma Only模式。

       > 不过AI提到“共线自旋极化的DFT计算中，自旋向上和向下的波函数是独立求解的，比如纯自旋向上的哈密顿量因为不涉及自旋翻转，这个算符依然是纯实数算符，便能满足共轭对称性”，似乎可以在这里做一些东西。

     - 题目5：使用Al和NaCl，首先用Al检测程序正确性；其次用大规模的NaCl检测优化后的代码是否有效优化内存占用，因为只有当数据量跨越CPU的L1和L2缓存容量时SIMD向量化对内存带宽的优化效果才能真正显现

     - 题目6：同题目4

     - 题目7：使用NaCl，用大体系来测试FFT里的MPI的Overlap优化是否能够有效提升性能

     - 题目8：使用Al和NaCl，首先用Al检测程序的懒加载机制是否生效，并确认缓存命中率；其次用NaCl检测在大体系下缓存优化的有效性

     - 附加题：同题目4

   - 计时指标：`distributeg`、`real2recip`、`recip2real`、`gather/scatter`、端到端总时间等

3. **预期指标**

   - 有关OpenMP的并行效率
     - 题目1的count_pw_st并行化、题目3的FFT数据拷贝与重排和
     - 通过并行提升计算效率，同时不能引入过多的线程同步开销
     - 预期在4线程下能够达到4x的加速比，当然这是理想情况

   - 有关MPI的通信效率和数据传输
     - 题目2的MPI非阻塞通信和题目7的FFT计算Overlap
     - 让通信和计算相重叠，提升整体效率
     - 预期达到最低2x的加速比

   - 有关Gamma Only模式的内存节省
     - 题目4、6和附加题
     - 利用时间反演对称性舍弃一半的倒空间数据，并在FFT前后通过共轭关系恢复数据
     - 预期能节省50%的内存消耗

   - 其他预期指标
     - 题目5平面波gather操作的向量化的加速能力提升
     - 题目8缓存优化的高缓存命中率和合理的内存开销

## 七、进度安排与分工

- 4.24（第10周）项目调研与计划书
  - A：梳理8题+附加题总体技术路线，列出核心模块与依赖
  - B：整理性能瓶颈与并行点（OpenMP/MPI/SIMD/GammaOnly）
  - C：整理测试体系与基准算例建议
  - 产出：项目计划书（已完成版）
- 5.15（第13周）详细算法文档
  - A：题1/3/5（OpenMP+SIMD）算法细化
  - B：题2/7（MPI非阻塞+重叠）算法细化
  - C：题4/6/附加题（GammaOnly扩展与紧凑存储）算法细化
  - 产出：详细算法文档（模块化说明、流程图、关键数据结构）
- 5.22（第14周）当前算法测试与总结
  - A：完成题1/3/5的测试用例与结果
  - B：完成题2/7的测试用例与结果
  - C：完成题4/6/附加题的正确性验证设计
  - 产出：阶段性测试报告（正确性+初步性能基线）
- 5.29（第15周）代码修改与重构报告
  - A：总结OpenMP/SIMD相关代码改动与重构点
  - B：总结MPI非阻塞与重叠改动与重构点
  - C：总结GammaOnly扩展与紧凑存储改动与重构点
  - 产出：代码修改/重构报告（含关键文件与函数）
- 6.5（第16周）代码优化报告
  - A：性能对比（OpenMP/SIMD方向）
  - B：性能对比（MPI重叠方向）
  - C：内存节省与GammaOnly效果评估
  - 产出：优化报告（加速比、瓶颈对比、改进分析）
- 6.12（期末）最终优化效果与总结
  - A：整合性能曲线与实验结果
  - B：整合正确性验证与回归测试
  - C：整合工程化总结与未来改进建议
  - 产出：最终报告与答辩材料

## 八、参考文献与资料

- 关于 ABACUS
  - [ABACUS 官方源代码仓库](https://github.com/abacusmodeling/abacus-develop)
  - [ABACUS 官方文档](https://abacus.deepmodeling.com/en/latest/)
  - [ABACUS 用户指南](https://mcresearch.github.io/abacus-user-guide/)
- 关于 FFT
  - [FFTW3 官方文档：Real-to-Complex/Complex-to-Real FFT](https://www.fftw.org/fftw3_doc/index.html#SEC_Contents)
- 关于 MPI
  - MPI Alltoallv 相关资料
  - 《MPI 并行程序设计》（都志辉）第12章 非阻塞通信 MPI 程序设计
- 关于 OpenMP
  - [SIMD 官方文档](https://www.openmp.org/spec-html/5.0/openmpsu42.html)
  - 《多核并行OpenMP计算》（雷洪）
- 平面波与 Gamma Only 相关理论教材与论文
- 其他网络资源
